{
  "name": "pb-days-game-of-crowns",
  "version": "1.0.0",
  "description": "PB Days collectible card rewards",
  "scripts": {
    "dev": "netlify dev",
    "build": "npm install",
    "inject-env": "node scripts/inject-env.js",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.2"
  }
}
