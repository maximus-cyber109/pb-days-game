{
  "name": "pb-days-game-of-crowns",
  "version": "1.0.0",
  "description": "PB Days collectible card rewards system",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build needed for static site'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
